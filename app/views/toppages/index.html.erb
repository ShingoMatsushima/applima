<div class="center jumbotron">
  <div class="text-center">
    <h1>アプリマ</h1>
    <h2>アイデアとエンジニアをマッチングさせるサービス</h2>

    <% if logged_in? %>
      <%= link_to 'アイデアを投稿する', new_idea_path, class: 'btn btn-lg btn-primary' %>
    <% else %>
      <%= link_to '新規登録する', signup_path, class: 'btn btn-lg btn-primary' %>
    <% end %>
  
  </div>
</div>
